$name;format="lower"$ = {
  dataSourceClass = "slick.jdbc.DriverDataSource"
  connectionTestQuery = "select 1"
  properties = {
    driver = "com.mysql.jdbc.Driver"
    url = "jdbc:mysql://localhost/test?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull"
    user = "root"
    password = "root"
  }
  numThreads = 10
}
gen = {
  dataSourceClass = "slick.jdbc.DriverDataSource"
  connectionTestQuery = "select 1"
  properties = {
    driver = "com.mysql.jdbc.Driver"
    url = "jdbc:mysql://localhost/test?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull"
    user = "root"
    password = "root"
  }
  numThreads = 10
  pkg = "$package$"
}

report {
  domain = "domain.com"
  from = "fromuser"
  password = "frompassword"
  receivers = ["touser1", "touser2"]
}
